# GitHub Personal Access Token (no scopes needed for public data).
# Create one at https://github.com/settings/tokens -> "Generate new token (classic)" -> no checkboxes -> Generate.
# Then copy it here and rename this file to ".env"
GITHUB_TOKEN=ghp_YOUR_TOKEN_HERE
# Optional: port for the local proxy (default 8787)
# PORT=8787
